UI prototype for premium dashboard (non-invasive).

To run:

1. cd frontend-dashboard/ui-prototype
2. npm install
3. npm run dev

This folder is a standalone prototype (Vite + Tailwind). It does not modify the main frontend-dashboard app and is intended for design iteration and integration planning.
